{
  "name": "rx-browser-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Claude Code ↔ browser extension relay. POC.",
  "license": "MIT",
  "workspaces": ["relay", "mcp"],
  "scripts": {
    "build": "npm run -ws build --if-present",
    "test": "node --test --import tsx tests/*.test.ts",
    "relay:dev": "npm -w relay run dev",
    "mcp:build": "npm -w mcp run build"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "puppeteer": "^23.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
